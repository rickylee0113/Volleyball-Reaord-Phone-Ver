import React from 'react';

// This component is currently deprecated as the action logic has been moved to GameView.tsx
// Keeping this file as a placeholder to avoid build errors if referenced elsewhere.
export const ActionPanel: React.FC = () => {
  return null;
};